# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Node.js app to Azure Web App - omniplex-arya-2025

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'

      - name: npm install, build, and test
env:
    NEXT_PUBLIC_FIREBASE_API_KEY="AIzaSyDQ3OL77SdEixpsDIvADL_Mgjgi-vgzBgM"
    NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="omniplex-b96d4.firebaseapp.com"
    NEXT_PUBLIC_FIREBASE_PROJECT_ID="omniplex-b96d4"
    NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="omniplex-b96d4.firebasestorage.app"
    NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="567668848925"
    NEXT_PUBLIC_FIREBASE_APP_ID="1:567668848925:web:2cf37c7cceda8b71b849f4"
    NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_51RnnLXIDjtD0nY0h61e3HLFp71sZwzNqvxH1cnlpUEF6YGTRoL4SUc46WLHPuygjbE88VIaIL8Ph199GGwiK3QXm00ZkCZ7hqY
    

run: |
    npm install --legacy-peer-deps
    npm run build --if-present

      - name: Zip artifact for deployment
        run: zip release.zip ./* -r

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: release.zip

    deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write #This is required for requesting the JWT
      contents: read #This is required for actions/checkout

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: node-app

      - name: Unzip artifact for deployment
        run: unzip release.zip
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_861F4A1D36EB4B9FA0479C14B41B19BC }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_33546C6D06214B0BA65F70933B9D37AB }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_396ED16E6B494893BF9CBAB745916CE2 }}

      - name: 'Deploy to Azure Web App'
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'omniplex-arya-2025'
          slot-name: 'Production'
          package: .
          
